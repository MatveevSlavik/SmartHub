import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#4f5a6e',
      dark: '#303b44',
    },
    secondary: {
      main: '#65c178',
    },
    text: {
      secondary: '#a7b3cb',
    },
  },
});

export default theme;
