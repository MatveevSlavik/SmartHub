import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
  root: {
    background: '#dee1e5',
  },
  layoutWrapper: {
    maxWidth: 1440,
    margin: theme.spacing(0, 'auto'), 
  },
  
}));
